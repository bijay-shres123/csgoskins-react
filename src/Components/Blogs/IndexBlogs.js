import React, { Component } from 'react';
import styled from 'styled-components';

class IndexBlogs extends Component {
    render() {
        return (
            <h1>This is Blog</h1>
        )
    };
}

export default IndexBlogs
